// Bibliotheken / Asembly Referenz / Namespace

using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using System.Linq;

//deklaration einer klasse

//public        -> acces modifier (zugriffsrechte) | keyword (vorreservierte Schlüsselwort)
//class         -> keyword zur Deklaration einer Klasse
//ExampleClass  -> idetnitfier (Name der Klasse)
//:MonoBehaviour-> Unterklasse von MonoBehaviour
public class ExampleClass : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
